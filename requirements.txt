# Pinned to the versions the reported numbers were produced with.
# Tested on Python 3.13. Python 3.14 is NOT supported: several of these have no
# 3.14 wheels yet, so `python run_all.py` fails at import on a 3.14 default.
#
# Verify before running:  python -c "import sys; print(sys.version)"

numpy==2.2.2
pandas==3.0.3
scipy==1.15.2
networkx==3.3
matplotlib==3.10.1

# Leiden community detection (Figure 10 algorithm comparison).
igraph==1.0.0
leidenalg==0.12.0
